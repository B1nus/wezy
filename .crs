operators:
+,-,*,/,%,sqrt,abs,float,ceil,floor,nearest,expand,trunc,rotl,rotr,shl,shr,clz,ctz,popcnt,<,>,<=,>=,==,!=,and,not,or,xor

types:
bool,u8,u16,u32,u64,i8,i16,i32,i64,f32,f64

library:
store(unn, u32)
store(inn, u32)
load(u32) unn
load(u32) inn
fd_write(u32,u32,u32,u32)
fd_read(u32,u32,u32,u32) u32
path_open(u32,u32,u32,u32) u32
proc_exit(u32)
memory_copy(u32,u32,u32)
memory_fill(u32,u32,u8)
memory_grow(u32)
memory_size() u32

literals:
x u32 = 1023980
x f32 = 10.38
x bool = true
ptr u32, len u32 = "hello!\n"

functions:
divrem(a u32, b u32) u32
  div u32 = a / b
  rem u32 = a % b
  ...
  return expression, expression

x u32, y u32 = expression, expression

loop name?
  ...
  repeat name?

if expression
  ...
else if expression
  ...

importing code:
use "relative/path/to/file.crs"

later:
Mutability of base types in function arguments is achieved by returning the values back from the function.
Hmmm... make top-level variables global variables? Is that what users expect?
