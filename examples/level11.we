x u32 = 5
x_str1 = format_reversed(x)
x_str2 = x.format_reversed()

assert(x_str1 == x_str2)

func format_reversed(num u32) [u8]
    out [u8]

    repeat
        out.append(num % 10)
        num = num / 10

        if num == 0
            break

    return out

