x u32 = 5
x_str1 = format_reversed(x)

function format_reversed(num u32) [u8]
    out [u8]

    repeat
        append(out, num % 10)
        num = num / 10

        if num == 0
            break

    return out

