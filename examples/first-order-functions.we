function map(list [s64], mutator function(s64) f64) [f64]
    new [f64]

    i = 1
    repeat length(list)
        new.append(list.get(i))
        i = i + 1

    return new

list = [ -1, 69, 420 ]
new = map(list, function(num s64) f64
    return num
)

assert(new == [ -1.0, 69.0, 420.0 ])
